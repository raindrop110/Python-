def bubble_sort(lst):
  for j in range(len(lst)):
    
    for i in range(len(lst)-1):
      if lst[i] > lst[i+1]:
        temp = lst[i]
        lst[i] = lst[i+1]
        lst[i+1] = temp
  
  return lst      
        
print(bubble_sort([5,3,4,1,2]))
